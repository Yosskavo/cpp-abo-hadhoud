#include <iostream>

using namespace std;

int main (void)
{
    int a;

    cout << "first number\n";
    cin >> a;
    cout << a * a << endl;
    cout << a * a * a << endl;
    cout << a * a * a * a << endl;
    return (0);
}
#include <iostream>

using namespace std;

int main (void)
{
    int billvalue;
    int x;

    cout << "the total bill ? \n";
    cin >> billvalue;
    x = billvalue * 1.1;
    cout << x * 1.16;
    return (0);
}